import { auth } from './auth';
import { collections } from './collections';

export class Service {
	constructor() {}
}

const service = {
	auth,
	collections,
};

export default service;
